# 中望技术社区自动签到（青龙面板版）

🤖 适配青龙面板的中望技术社区每日自动签到脚本，获取积分奖励。

## 📋 更新日志

### v3.5.0 (2025-06-26)
- ✅ **增加token有效性验证**：避免使用无效的b2_token
- 🔑 **优先使用access_token验证**：确保登录状态真实有效
- 🐛 **修复"登录成功但签到失败"的问题**：所有登录方式都经过API验证
- 📈 **提高登录可靠性**

### v3.4.0 (2025-06-26)
- 🔄 **重构登录流程**：采用多方案降级策略
- 🍪 **方案A（优先）**：完成所有重定向后用cookie模式登录
- 🔑 **方案C（备用）**：重新发起授权请求获取新的授权码
- 📈 **提高登录成功率和稳定性**

### v3.3.0 (2025-06-26)
- 🐛 **修复授权码 invalid_grant 错误**：发现授权码后立即停止重定向
- ⚡ **优化重定向跟踪逻辑**：避免code被论坛回调页面消耗
- 📈 **提高token获取成功率**

### v3.2.0 (2025-06-26)
- 🐛 **修复b2_token获取问题**：增加多步重定向跟踪，每步检查cookie
- 🍪 **新增cookie模式**：支持通过session cookie直接调用API
- 🏠 **优化登录流程**：登录前先访问论坛首页建立会话
- 🔍 **增加备用方案**：从页面HTML中提取b2_token
- 📊 **优化错误诊断**：更详细的调试日志

### v3.1.0 (2025-06-25)
- 🐛 **修复登录跳转URL解码问题**：需要两次 `decodeURIComponent` 解码
- 🔧 **增加授权码获取容错处理**：多种方式检测授权码
- ⚡ **优化b2_token获取**：支持直接使用access_token调用API
- 🛡️ **改进Selenium模式错误处理**：更友好的错误提示

### v3.0.0 (2025-06-25)
- ✨ **修复API登录模式**：使用正确的 `/Account/UserLogin` 接口
- 🔐 **添加RSA密码加密**：与JSEncrypt兼容，使用PKCS1_v1_5填充
- ⚡ **优化登录流程**：提高登录成功率
- 📝 **完善错误处理**：更详细的错误信息和日志输出
- 📦 **新增依赖**：pycryptodome（API模式必需）

## ✨ 功能特点

- ✅ **双模式运行**：API 模式（轻量快速）+ Selenium 模式（备用兼容）
- ✅ **多账号支持**：支持多账号批量签到
- ✅ **青龙面板适配**：标准青龙脚本格式，支持环境变量配置
- ✅ **青龙通知推送**：内置青龙通知模块，支持多渠道推送
- ✅ **通知级别控制**：0=关闭 1=仅异常 2=全部通知
- ✅ **调试模式**：支持 DEBUG 环境变量调试
- ✅ **自动降级**：API 模式失败自动降级到 Selenium 模式

## 📦 快速开始

### 方式一：ql repo 拉取（推荐）

在青龙面板「定时任务」中新建任务：

```
ql repo https://github.com/Hayfan-wu/QL-ZW-CHECKIN.git "zw_" "" "" "main"
```

### 方式二：手动上传

1. 下载 `zw_checkin.py` 文件
2. 上传到青龙面板的脚本目录

## ⚙️ 环境变量配置

在青龙面板「环境变量」中添加以下变量：

| 变量名 | 说明 | 必填 |
|--------|------|------|
| `ZWSOFT_USERNAME` | 中望社区账号（手机号/邮箱），多账号换行分隔 | ✅ |
| `ZWSOFT_PASSWORD` | 中望社区密码，多账号换行分隔（与账号一一对应） | ✅ |
| `ZWSOFT_NOTIFY` | 通知级别：0=关闭 1=仅异常 2=全部通知（默认1） | ❌ |
| `ZWSOFT_DEBUG` | 调试模式：true/false（默认false） | ❌ |
| `ZWSOFT_MODE` | 运行模式：api/selenium（默认api） | ❌ |

### 多账号配置示例

**ZWSOFT_USERNAME:**
```
13800138000
13900139000
```

**ZWSOFT_PASSWORD:**
```
password123
password456
```

> 💡 账号和密码按顺序一一对应，第1行账号对应第1行密码

## 🚀 运行模式说明

### API 模式（默认，推荐）

- 优点：轻量快速，无需安装浏览器，资源占用少
- 缺点：可能受 API 变更影响
- 依赖：`requests` + `pycryptodome`

环境变量设置：
```
ZWSOFT_MODE=api
```

> 💡 API 模式需要安装 pycryptodome：
> ```
> pip install pycryptodome
> ```
> 青龙面板中需要在「依赖管理」→「Python3」中添加

### Selenium 模式（备用）

- 优点：模拟真实浏览器，兼容性好
- 缺点：需要安装 Chrome 浏览器和相关依赖，资源占用大

环境变量设置：
```
ZWSOFT_MODE=selenium
```

> 💡 Selenium 模式需要额外安装依赖：
> ```
> pip install selenium webdriver-manager
> ```
> 青龙面板中需要在「依赖管理」→「Python3」中添加

## 🔔 通知推送

脚本内置青龙面板通知模块，支持以下推送渠道（在青龙面板配置文件中设置）：

- Server酱
- 企业微信机器人/应用
- 钉钉机器人
- Bark
- Telegram Bot
- PushPlus
- 飞书机器人
- iGot
- WxPusher

### 通知级别控制

通过 `ZWSOFT_NOTIFY` 环境变量控制：

- `0`：关闭所有通知
- `1`（默认）：仅异常时通知（签到失败、脚本错误）
- `2`：每次运行都通知

## 📋 定时任务

在青龙面板「定时任务」中创建：

- **任务名称**：中望社区签到
- **命令**：`task zw_checkin.py`
- **定时规则**：`0 0 1 * * *`（每天凌晨1点）

## 🧪 手动测试

1. 在青龙面板「脚本管理」中找到 `zw_checkin.py`
2. 点击「运行」按钮
3. 查看运行日志确认结果

## 🔧 常见问题

### Q: API 模式登录失败怎么办？

A: 尝试切换到 Selenium 模式：
```
ZWSOFT_MODE=selenium
```

### Q: 多账号怎么配置？

A: 在环境变量中每行一个账号，密码同理，顺序一一对应。

### Q: 收不到通知？

A: 1. 检查青龙面板的通知配置是否正确
   2. 确认 `ZWSOFT_NOTIFY` 不是 0
   3. 查看脚本日志中是否有通知发送失败的提示

### Q: 签到失败提示验证码？

A: 中望社区登录有滑块验证码，建议使用 API 模式。如果 API 模式不可用，可以尝试 Selenium 模式，但可能无法绕过验证码。

## 📝 签到规则

根据中望社区签到说明：

- 每日签到成功后，可随机获得 **30-100 积分**
- 每连续签到 7 天额外奖励 **200 积分**
- 如签到中途中断，则连续签到重新开始计算

## ⚠️ 注意事项

1. 请确保您的中望社区账号密码正确
2. 建议定期检查签到运行状态
3. 本工具仅供学习交流使用
4. 请勿滥用，遵守网站服务条款

## 📄 许可证

MIT License
